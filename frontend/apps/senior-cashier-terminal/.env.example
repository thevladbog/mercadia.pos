# Store Edge — leave empty in local dev to use the Vite proxy (:8081)
VITE_STORE_EDGE_URL=

# Hardware Agent — leave empty in local dev to use the Vite proxy (:8083)
VITE_HARDWARE_AGENT_URL=

# Store ID — must match a known store in the system
VITE_STORE_ID=store-1
