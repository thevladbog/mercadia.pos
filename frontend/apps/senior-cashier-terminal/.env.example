# Store Edge — local operational API (default :8081)
VITE_STORE_EDGE_URL=http://localhost:8081

# Hardware Agent — iButton / MSR / fiscal / payment devices (default :8083)
VITE_HARDWARE_AGENT_URL=http://localhost:8083

# Store ID — must match a known store in the system
VITE_STORE_ID=store-1
